# Spring Boot Configuration
server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: postcards-service
  datasource:
    url: jdbc:h2:mem:postcardsdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    org.common.services: DEBUG
    org.springframework: INFO
